<% if(isAuthenticated) {%>
  <form action="/cart" method="POST">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <input type="hidden" name="productId" id="productId" value="<%= data._id %>">
    <button type="submit" class="btn">Add to Cart</button>
  </form>
<%}%>
